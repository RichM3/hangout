import { connect } from 'react-redux';
import DiscussionsIndex from './discussions_index';

const mapStateToProps = (state, ownProps) => {

    return ({
    })
};

const mapDispatchToProps = (dispatch) => {
    return ({
    })
};

export default connect(mapStateToProps, mapDispatchToProps)(DiscussionsIndex);
