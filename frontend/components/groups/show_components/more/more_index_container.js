import { connect } from 'react-redux';
import MoreIndex from './more_index';

const mapStateToProps = (state, ownProps) => {

    return ({
    })
};

const mapDispatchToProps = (dispatch) => {
    return ({
    })
};

export default connect(mapStateToProps, mapDispatchToProps)(MoreIndex);
